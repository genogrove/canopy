# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments (uv.lock IS committed — it pins the build for reproducibility)
.venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.idea/
.vscode/
.DS_Store

# Secrets
.env

# Raw genomic data downloaded during data prep (root-level only, so src/ask/data/
# catalogs stay tracked). Datasets are pinned in ask.resources, not committed here.
/*.bed
/*.bed.gz
/*.bed.gz.tbi
/*.osc.txt.gz
/*.obo.txt
/*.gff3.gz
/F5.hg38.*
/human_permissive_*
/hg19.cage_*
re2g_raw/
re2g_consolidated/
enh_index/
re2g_index/
# Scratch for data prep / upload staging (e.g. the cCRE hosted-pair before it's pushed).
tmpfiles/
# ponytail: design notes untracked for now — commit docs/showcase-design.md once it settles.
docs/
